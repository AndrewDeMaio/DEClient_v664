#ifndef __UI_TYPEDEF__
#define __UI_TYPEDEF__

#include <limits.h>
#include "typedef.h"


typedef void *    WINDOWHANDLE;	// handle of window

////////////////////////////////////////////////////////////////////////////
typedef UINT      HANDLEID;

const UINT INVALID_HID	= UINT_MAX;
////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////
typedef UINT      WINDOW_ID;

const UINT	INVALID_WID = UINT_MAX;
////////////////////////////////////////////////////////////////////////////

typedef UINT      EXEC_ID; // 메뉴 항목 실행 id

#endif